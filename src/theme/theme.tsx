import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#5a6eda",
    },
    secondary: {
      main: "#ffffff",
    },
    background: {
      default: "#f1efef",
    },
  },
});

export default theme;
